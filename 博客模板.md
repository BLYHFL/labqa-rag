# LabQA：基于 Multi-Agent RAG 架构的实验室智能问答系统实践

## 写在前面

在AI实验室里，团队成员每天都会遇到大量重复性问题："GPU服务器还有空卡吗？""自动驾驶测试车谁在用？""DPO论文的核心方法是什么？""XX项目的进度怎么样？"

这些问题的答案通常散落在各个群聊记录、文档、表格和个人的记忆里。新人来了要问一遍，老人也要频繁翻聊天记录。于是我们做了 **LabQA**——一个轻量级的实验室智能问答系统，用 Multi-Agent + RAG 的架构，把知识库和 LLM 结合起来，让机器人来回答这些日常问题。

---

## 项目背景

实验室大约20人，日常涉及：

- **设备管理**：GPU服务器、工作站、自动驾驶测试车、采集套件等硬件资产
- **项目管理**：学术论文、外勤测试、技术预研等多个并行项目
- **知识积累**：论文笔记、技术调研、流程规范等文档沉淀

知识形态非常多样：Word/PPT/Excel/PDF/Markdown 混在一起，存放位置也不统一。

**核心诉求**：一个能"读懂"知识库并回答日常问题的机器人，部署简单，不需要额外的数据库或后端服务。

---

## 快速上手：如何运行项目

整个项目从零启动只需要几分钟，核心就三步：**装依赖 → 配 Key → 跑起来**。

### 第一步：环境准备与安装

```bash
# 确保 Python 3.9+
python3 --version

# 克隆项目（内部项目，拿到源码目录即可）
cd labqa-question-answering

# 安装核心依赖（仅需两个库）
pip install -r requirements.txt
```

依赖说明：

| 依赖 | 用途 | 是否必装 |
|------|------|---------|
| `httpx` | LLM API 调用（HTTP 客户端） | ✅ 必装 |
| `lark-oapi` | 飞书 WebSocket 长连接 | ⚠️ 只用飞书时需要 |

> 文档转换功能（Word/PPT/Excel 转 Markdown）需要额外装：`pip install python-docx python-pptx openpyxl`

### 第二步：配置 LLM API

```bash
# 复制配置模板
cp .env.example .env

# 编辑 .env，填入你的 API Key
vim .env
```

最小的有效配置只需要一个 Key：

```ini
# .env 文件
LABQA_LLM_API_KEY=sk-your-api-key-here
```

切换模型也很方便，改几行配置就行：

```ini
LABQA_LLM_PROVIDER=deepseek    # 或 openai
LABQA_LLM_API_BASE=https://api.deepseek.com
LABQA_LLM_MODEL=deepseek-chat  # 或 gpt-4o
```

> **环境变量优先**：在 Docker/CI 环境下，直接 `export` 会覆盖 `.env` 中的值，方便不同环境差异化配置。

### 第三步：运行系统

系统支持**三种运行模式**，覆盖开发、验证、生产全场景：

#### ① CLI 交互模式（推荐开发调试）

```bash
python3 main.py cli
```

进入交互式终端，可以连续提问：

```
💬 LabQA CLI 交互模式（输入 'quit' 退出）
支持设备 / 项目 / 知识三类问题

你: GPU服务器在哪
🤖 GPU-A100-01 在 机柜A-3，从上往下第4U ...
```

#### ② 单次查询模式（快速验证）

```bash
python3 main.py ask "自动驾驶测试车谁在用"
```

适合脚本调用、CI 集成或快速验证知识库内容，一次一问一答。

#### ③ 飞书长连接模式（生产推荐）

这是最推荐的生产部署方式，**不需要公网 IP**，非常适合内网实验室：

```bash
python3 main.py feishu
```

启动后机器人会常驻后台，自动响应飞书群里的 @消息。

接入飞书需要先配置凭证：

```
1. 登录 飞书开放平台 → 创建企业自建应用
2. 添加「机器人」能力，开启 im:message 权限
3. 在「事件订阅」中选择「使用长连接接收事件」
4. 将 FEISHU_APP_ID 和 FEISHU_APP_SECRET 填入 .env
```

### 第四步：填充知识库

系统启动后，知识库就是一组 Markdown 文件，按分类放在不同目录下：

```
.opencode/context/
├── devices/       ← 设备信息（GPU、服务器、测试车…）
├── projects/      ← 项目信息（论文、路测、预研…）
├── knowledge/     ← 知识文档（论文笔记、技术调研…）
└── guides/        ← 流程规范（入职指南、代码规范…）
```

**直接编辑 Markdown** 是最灵活的方式：

```bash
# 用任意编辑器修改
vim .opencode/context/devices/设备总览.md
```

**批量导入**已有文档也很方便：

```bash
# 1. 把 Word/PPT/Excel 丢入 raw-docs/
cp ~/论文笔记.docx raw-docs/

# 2. 运行自动转换脚本
bash scripts/convert.sh

# 3. 脚本会自动识别文件类型、转换格式、归入对应目录
```

转换脚本会自动识别文件名关键词（如"设备""GPU""项目""规范"等）来决定归属，并自动添加 YAML frontmatter。

### 第五步：验证运行

```bash
# 测试设备查询
python3 main.py ask "有哪些GPU服务器"

# 测试项目查询
python3 main.py ask "论文项目进度"

# 测试知识查询
python3 main.py ask "新人入职流程"
```

还没配 API Key？没关系。CLI 会自动进入 **离线模式**，只展示检索到的知识库内容而不调用 LLM——这在调试知识库时非常有用：

```
📚 检索到的知识库内容（离线模式，未调用 LLM）：
...
```

---

## 系统架构

LabQA 的核心架构是一个 **Multi-Agent RAG 系统**，整体流程如下：

```
用户提问 → 意图识别 → 路由到子Agent → 检索知识库 → LLM 生成回答
```

### 架构总览

```
┌─────────────────────────────────────────────────────┐
│                   飞书 / CLI 消息入口                  │
└────────────────────────┬─────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────┐
│              Lab-Orchestrator (主调度器)               │
│           意图识别 → 路由选择 → 结果整合                 │
└──────┬──────────────────┬──────────────────┬─────────┘
       ▼                  ▼                  ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ Device-Agent │  │Project-Agent │  │Knowledge-Agent│
│   设备查询    │  │   项目查询    │  │   知识查询    │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       └─────────┬───────┴─────────┬───────┘
                 ▼                 ▼
┌─────────────────────────────────────────────────────┐
│              📚 统一知识库 (Markdown 文件)              │
│  devices/  │  projects/  │  knowledge/  │  guides/   │
└─────────────────────────────────────────────────────┘
                          ▲
┌─────────────────────────────────────────────────────┐
│        🔄 文档预处理管道 (Word/PPT/Excel → Markdown)    │
└─────────────────────────────────────────────────────┘
```

### 三个核心 Agent

| Agent | 职责 | 知识目录 |
|-------|------|---------|
| **Device-Agent** | 设备/资产查询（位置、状态、负责人） | `context/devices/` |
| **Project-Agent** | 项目信息查询（进度、人员、里程碑） | `context/projects/` |
| **Knowledge-Agent** | 知识文档查询（论文、规范、教程） | `context/knowledge/` + `context/guides/` |

---

## 核心技术设计

### 1. 关键词驱动的意图识别

我们没有用 LLM 来做意图识别（太慢也费钱），而是采用**纯关键词打分机制**：

```python
# 设备类关键词
DEVICE_KEYWORDS = [
    "设备", "gpu", "a100", "服务器", "显卡", "空闲",
    "谁在用", "在哪", "维修", "测试车", ...
]

# 项目类关键词
PROJECT_KEYWORDS = [
    "项目", "进度", "负责人", "里程碑", "icml", "neurips", ...
]

# 知识类关键词
KNOWLEDGE_KEYWORDS = [
    "论文", "文档", "流程", "规范", "怎么做", "dpo", ...
]
```

打分规则很简单：
- 关键词在文本中出现：短词 +1，长词 +2
- 分词精确匹配额外 +1
- 取最高分的意图为主意图

如果多个意图分数接近（>50%），还会触发**混合查询模式**，同时路由到多个 Agent 并整合结果。

### 2. 文件即知识库

知识库就是一组带 YAML frontmatter 的 Markdown 文件：

```markdown
---
type: device
tags: [GPU, 算力, NVIDIA]
updated: 2026-05-07
author: 张三
---

# GPU-A100-01

- **型号**: NVIDIA A100 80GB
- **位置**: 3号机房 A-03 机柜
- **状态**: 4/8 卡空闲
- **负责人**: 张三
```

这种设计的好处：
- 人类可读，版本控制友好
- LLM 对 Markdown 的理解效果最佳
- 管理员可以直接用编辑器修改
- 支持批量导入：Word/PPT/Excel → 预处理脚本 → Markdown

### 3. RAG 检索管道

每个 Agent 的 `answer()` 方法遵循统一的 RAG 流程：

```
提取关键词 → 搜索知识库 → 拼接上下文 → 调用 LLM → 返回回答
```

核心检索逻辑在 `ContextStore` 中：

```python
def search(self, keywords, category, top_k=5):
    # 1. 分类过滤
    candidates = [f for f in files if f.category == category]
    
    # 2. 关键词打分
    scored = [(f, f.matches_keywords(keywords)) for f in candidates]
    
    # 3. 文件名匹配加权 (+10分)
    # 4. 内容匹配按频次计分 (每个匹配 +2, 上限10)
    # 5. frontmatter tags 匹配额外 +5
    
    return sorted(scored, key=lambda x: x[1], reverse=True)[:top_k]
```

### 4. 双重去重机制

飞书的消息推送有时会重复（3秒超时重推），我们在 `feishu_ws.py` 中实现了**双重去重**：

```python
# 第1层：message_id 去重
if msg_id in processed_ids:
    return True  # 跳过

# 第2层：同一群聊 + 相同内容 + 30秒窗口内去重
text_hash = md5(text).hexdigest()
key = f"{chat_id}:{text_hash}"
if key in content_cache and (now - content_cache[key]) < 30:
    return True  # 跳过
```

---

## 工程实践亮点

### 1. 三种运行模式

通过 `main.py` 统一入口，支持三种运行模式：

```bash
python3 main.py cli         # 命令行交互模式（开发调试）
python3 main.py ask "问题"   # 单次查询模式（快速验证）
python3 main.py feishu      # 飞书长连接模式（生产使用）
```

特别值得一提的是 **飞书长连接模式**——基于 lark-oapi SDK 的 WebSocket 方案，不需要公网 IP，非常适合内网部署的实验室场景。

### 2. 离线模式

没有配置 LLM API Key 时，CLI 会自动进入离线模式，只做知识检索不调 LLM，方便在没有网络的环境下验证知识库内容：

```python
# 离线模式：仅展示检索到的知识库内容
print("📚 检索到的知识库内容:\n")
print(context[:3000])
```

### 3. LLM 客户端抽象

`LLMClient` 抽象了 LLM 调用，支持 DeepSeek / OpenAI / 任意兼容 API，统一的 `chat_with_context()` 方法封装了 System Prompt 注入：

```python
def chat_with_context(self, agent_role, context_text, user_query):
    system_prompt = f"""{agent_role}

## 知识库内容
{context_text}

## 回答规范
1. 直接回答，禁止开场白
2. 只输出关键信息
3. 优先用简洁列表
4. 引用知识来源"""
    
    return self.chat(system_prompt, user_query)
```

### 4. Config 管理的巧妙之处

配置管理是手写的（没有用 python-dotenv），但处理得很细致：

- .env 文件 vs 环境变量：**环境变量优先级更高**，方便 Docker/CI 场景
- 多 Key 兼容：`LABQA_LLM_API_KEY` 或 `DEEPSEEK_API_KEY` 均可
- URL 构造的防御性处理：避免 `/v1/v1/chat/completions` 这种路径重复问题

---

## 项目结构

```
labqa-question-answering/
├── main.py                   # 统一入口
├── labqa/
│   ├── orchestrator.py       # 主调度器
│   ├── router.py             # 意图识别引擎
│   ├── context_store.py      # 知识库检索
│   ├── llm.py                # LLM 客户端
│   ├── config.py             # 配置管理
│   ├── cli.py                # 命令行交互
│   ├── feishu_ws.py          # 飞书长连接
│   └── agents/
│       ├── base.py           # Agent 基类
│       ├── device_agent.py   # 设备查询
│       ├── project_agent.py  # 项目查询
│       └── knowledge_agent.py # 知识查询
├── .opencode/context/        # 知识库
│   ├── devices/
│   ├── projects/
│   ├── knowledge/
│   └── guides/
├── raw-docs/                 # 原始文档
├── scripts/convert.sh        # 文档预处理
└── docs/
    ├── ARCHITECTURE.md
    └── TESTING.md
```

---

## 一些技术决策 (ADR)

### 为什么用 Markdown 而不是向量数据库？

MVP 阶段，团队只有~20人，知识库规模不大（几十个文件），用向量数据库（Chroma/Pinecone）太重了。Markdown 文件+关键词搜索足够应对当前的检索需求，维护成本几乎为零。

### 为什么用关键词打分而不是 LLM 做意图识别？

LLM 做意图识别准确率高但延迟大、成本高。对于"设备""项目""论文"这几个明确类别，关键词打分足够准确且几乎是零延迟。实测响应时间 < 200ms（不含 LLM 回答生成）。

### 为什么同时支持飞书和 CLI？

飞书是日常使用入口，CLI 是开发和调试入口。两种模式共享同一套 Agent 逻辑，便于本地验证后再上线。

---

## 总结与展望

LabQA 是一个**小而美的实用系统**，解决了实验室日常的信息获取痛点。它的核心设计哲学是：**用最简单的方式解决问题**。

当前 MVP 已经跑通了完整链路，后续可以迭代的方向：

1. **知识库增量更新**：目前需要手动运行预处理脚本，后续可以加文件监听自动更新
2. **对话记忆**：支持多轮对话上下文
3. **更丰富的知识源**：接入飞书文档、Wiki 等在线知识库
4. **更多 Agent**：如排期查询、请假查询等

---

> 项目代码：[LabQA](https://github.com/your-repo/labqa) （内部项目）
>
> 技术栈：Python + DeepSeek API + lark-oapi SDK + Markdown
>
> 欢迎交流讨论！
