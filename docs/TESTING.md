# LabQA 测试清单

## 测试环境准备

- [ ] 知识库已填充至少以下文件：
  - [ ] `context/devices/设备总览.md`（至少包含1台设备）
  - [ ] `context/projects/项目总览.md`（至少包含1个项目）
  - [ ] `context/guides/新人入职指南.md`（实际内容）
  - [ ] `context/guides/代码提交规范.md`（实际内容）
- [ ] 文档转换依赖已安装: `pip install python-docx python-pptx openpyxl`

---

## 1. Orchestrator 路由测试

### 1.1 意图识别 - 设备类
- [ ] 输入: "GPU服务器在哪" → 应路由到 Device-Agent
- [ ] 输入: "A100还有空闲的吗" → 应路由到 Device-Agent
- [ ] 输入: "机房有哪些设备" → 应路由到 Device-Agent

### 1.2 意图识别 - 项目类
- [ ] 输入: "XX项目进展如何" → 应路由到 Project-Agent
- [ ] 输入: "谁负责数据采集" → 应路由到 Project-Agent
- [ ] 输入: "论文项目的里程碑是什么" → 应路由到 Project-Agent

### 1.3 意图识别 - 知识类
- [ ] 输入: "那篇Transformer论文讲了什么" → 应路由到 Knowledge-Agent
- [ ] 输入: "怎么提交代码" → 应路由到 Knowledge-Agent
- [ ] 输入: "新人入职流程" → 应路由到 Knowledge-Agent

### 1.4 意图识别 - 无法识别
- [ ] 输入: "今天天气怎么样" → 应返回引导提示

---

## 2. Device-Agent 测试

### 2.1 精确查询
- [ ] 输入: "/查设备 GPU服务器" → 返回GPU服务器的位置、状态、负责人
- [ ] 输出格式符合 Device-Agent 的 output_format 规范

### 2.2 模糊查询
- [ ] 输入: "/查设备 GPU" → 返回所有GPU设备列表
- [ ] 输入: "/查设备 空闲" → 返回空闲设备列表

### 2.3 无结果查询
- [ ] 输入: "/查设备 不存在的设备名" → 返回"未找到"提示，附建议

---

## 3. Project-Agent 测试

### 3.1 精确查询
- [ ] 输入: "/查项目 论文项目" → 返回项目阶段、负责人、里程碑
- [ ] 输出格式符合 Project-Agent 的 output_format 规范

### 3.2 人员查询
- [ ] 输入: "/查项目 张三" → 返回张三参与的所有项目

### 3.3 类型筛选
- [ ] 输入: "/查项目 外勤" → 返回外勤类项目列表

---

## 4. Knowledge-Agent 测试

### 4.1 流程查询
- [ ] 输入: "/查文档 入职流程" → 返回新人入职指南的分步内容
- [ ] 输入: "/查文档 代码规范" → 返回代码提交规范

### 4.2 知识查询
- [ ] 输入: "/查文档 Transformer" → 返回论文笔记内容
- [ ] 输出引用来源（文件名）

### 4.3 无结果查询
- [ ] 输入: "/查文档 不存在的文档" → 返回"未找到"提示

---

## 5. 文档转换管道测试

### 5.1 Word 转换
- [ ] 在 `raw-docs/` 放入一个 .docx 文件
- [ ] 运行 `bash scripts/convert.sh`
- [ ] 验证 `context/` 下生成了对应的 .md 文件
- [ ] 验证 .md 文件包含 frontmatter

### 5.2 Excel 转换
- [ ] 在 `raw-docs/` 放入一个 .xlsx 文件
- [ ] 运行 `bash scripts/convert.sh`
- [ ] 验证转换结果包含 Markdown 表格

### 5.3 PPT 转换（需 python-pptx）
- [ ] 在 `raw-docs/` 放入一个 .pptx 文件
- [ ] 运行 `bash scripts/convert.sh`
- [ ] 验证转换结果包含幻灯片文字

### 5.4 已存在文件处理
- [ ] 再次运行 `bash scripts/convert.sh`
- [ ] 验证已存在的文件被跳过（不会重复创建）

### 5.5 空目录处理
- [ ] 清空 `raw-docs/`
- [ ] 运行 `bash scripts/convert.sh`
- [ ] 验证输出"目录为空"提示

---

## 6. 飞书 Webhook 测试

### 6.1 本地启动
- [ ] 启动 `python3 scripts/feishu-webhook.py`
- [ ] 访问 `http://localhost:8080/health` 返回 `{"status": "ok"}`

### 6.2 URL 验证
- [ ] 发送 URL 验证请求 → 返回 challenge

### 6.3 消息处理
- [ ] 发送文本消息事件 → 返回意图识别结果

---

## 7. 端到端测试

### 7.1 完整问答链路
- [ ] 飞书发送消息 → Webhook 接收 → 意图识别 → 路由 Agent → 返回结果
- [ ] 各环节日志正常

### 7.2 并发测试
- [ ] 同时多个用户提问 → 各收到准确回答

---

## 8. 回归测试清单

每次更新知识库后验证：
- [ ] `/查设备` 返回最新设备信息
- [ ] `/查项目` 返回最新项目状态
- [ ] `/查文档` 返回最新文档内容
- [ ] 新转换的文档可被正确检索

---

## 缺陷记录

| 编号 | 描述 | 严重程度 | 状态 |
|------|------|---------|------|
| - | - | - | - |

---

*测试完成后请更新日期和签名*
