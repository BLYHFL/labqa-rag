# LabQA 依赖
# 安装: pip install -r requirements.txt

# LLM API 调用
httpx>=0.25.0

# 飞书长连接模式（WebSocket，无需公网IP）
lark-oapi>=1.6.0

# 文档转换（可选）
# python-docx>=1.0.0   # Word 转 Markdown
# python-pptx>=0.6.0   # PPT 转 Markdown
# openpyxl>=3.0.0      # Excel 转 Markdown
